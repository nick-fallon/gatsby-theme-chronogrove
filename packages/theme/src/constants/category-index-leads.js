/** Fallback intros for category index pages when `siteMetadata` omits the lead (keep strings aligned with `theme-config.js` defaults). */
export const DEFAULT_BLOG_INDEX_LEAD = 'Posts from the blog.'
export const DEFAULT_MUSIC_INDEX_LEAD = 'Original songs, covers, and audio posts.'
