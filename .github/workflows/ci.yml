name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  lint:
    name: Lint and Format
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Install pnpm
        uses: pnpm/action-setup@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version-file: '.nvmrc'
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Run Prettier check
        run: pnpm run prettier

      - name: Run ESLint
        run: pnpm run lint

  test:
    name: Unit Tests with Coverage
    runs-on: ubuntu-latest
    needs: lint

    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Install pnpm
        uses: pnpm/action-setup@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version-file: '.nvmrc'
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Run tests and collect coverage
        run: pnpm exec turbo run test:coverage --concurrency=1 -- --maxWorkers=2

      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v5
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          slug: ${{ github.repository }}
          files: packages/theme/coverage/lcov.info,packages/ui/coverage/lcov.info
          flags: unittests
          fail_ci_if_error: true
          disable_search: true
          working-directory: ${{ github.workspace }}

      - name: Run tests for Codecov analytics
        run: pnpm exec turbo run test:codecov

      - name: Upload test results to Codecov
        if: ${{ !cancelled() }}
        uses: codecov/codecov-action@v5
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          slug: ${{ github.repository }}
          report_type: test_results
          files: packages/theme/junit.xml
          disable_search: true
          working-directory: ${{ github.workspace }}

  # Optional: Add this if you have a build step you want to verify
  build:
    name: Build
    runs-on: ubuntu-latest
    needs: [lint, test]

    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Install pnpm
        uses: pnpm/action-setup@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version-file: '.nvmrc'
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Load env vars from .env.template
        run: |
          echo "⚙️ Loading mock env vars from .env.template"
          grep -v '^#' .env.template | grep '=' >> $GITHUB_ENV
          echo "✅ Environment variables loaded from .env.template"

      - name: Build project
        run: pnpm exec turbo run build --filter=www.chrisvogt.me
